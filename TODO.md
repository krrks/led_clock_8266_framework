# TODO

## 显示

- [ ] 日期格式 `09-15` 中间的 `-` 没有显示出来。疑似 [FontData](src/FontData.h) 字体缺少 `-` 字形(或 drawChar 未处理该字符),需检查后补字形或改分隔符。
- [ ] IP 显示从滚动改为**翻页轮播**:先显示 `192.168` 停留一段时间,再显示 `3.37`,如此循环(替代 drawScroll 滚动方式)。暂不实施。

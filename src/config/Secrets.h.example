#pragma once
// Secrets.h.example — copy this file to Secrets.h and fill in your values.
// Secrets.h is gitignored and stays local; this example is what gets shared.

#define WEATHER_API_KEY       "your_openweathermap_api_key"
#define WIFI_DEFAULT_SSID     "your_wifi_ssid"
#define WIFI_DEFAULT_PASSWORD "your_wifi_password"
